import React from 'react';

const Styles = () => {
  return (
    <div>
      <button className="btn primary">Editar perfil</button>
      <button className="btn secondary">Editar perfil</button>
      <button className="btn animate">Editar perfil</button>
    </div>
  );
};

export default Styles;
